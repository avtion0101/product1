# 验证说明

日期：2026-09-24。生成器版本：1.2.0。

## 本轮交付

- 保留一键生成器，并将最新的“食品添加剂超量使用检测系统”作为可直接运行的项目纳入仓库。
- 项目使用业务表单、状态卡片、规则结果和可视化页面，不要求最终用户编辑 JSON 树。
- Windows 环境下，项目的 93 项单元测试和生成器的 11 项自检均已通过；PyQt 界面已进行离屏启动检查。
- 示例食品、配方与限量规则仅用于演示流程，不应当作为真实食品安全合规结论。

## 上轮问题闭环

- 旧版生成结果容易与最新版混淆：已清理三个过期输出，仅保留最新一次生成结果及回归测试夹具；生成结果不上传仓库。
- Windows 上 Python 命令可能被系统别名干扰：启动脚本增加了可用解释器检测和明确的报错提示。
- 原始结构化输入不符合标准软件的操作习惯：食品添加剂项目改用专用业务界面，并将这一要求写入生成提示词。
- 新增 macOS 启动脚本，优先使用 Python 3.12，检查最低 Python 版本，并只安装有预编译轮子的依赖。

## Mac 验证边界

Apple 芯片所需依赖有对应的 macOS 预编译发行包，安装和启动步骤见根目录 README。当前验证环境是 Windows，尚未在用户的 Mac 上实际启动，因此不将 Mac 运行标记为已实测通过。
