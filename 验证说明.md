# 验证说明

验证日期：2026-09-22　版本：1.1.0

## 已完成验证

- 已从 `D:\基础项目生成办法` 的 5 个案例提取源码规模、文档页数、模块特征和可复用约束。
- 本机 Codex 已登录 ChatGPT；`codex exec` 结构化 JSON 连接测试成功，实测 127.7 秒，输入 16990 Token、输出 15 Token。
- 生成器使用 Tkinter 启动，不依赖 PyQt6；界面初始化测试通过。
- Python 全量语法编译通过，11 项本地契约、跨平台启动、模板回退和模拟API测试全部通过。
- Microsoft Word 模板转换通过；旧二进制 Word 模板只转换副本，不修改原文件。
- 1.1.0改造后再次实际执行DOCX到PDF转换，确认Windows Microsoft Word回退仍然可用；多提供商Tk界面以Grok配置完成无交互初始化。
- 文档联调生成操作手册 37 页、代码文档 65 页，自动检查错误为 0。
- 代码文档的 3250 个正文显示行与源码行映射逐行一致；65 页 PDF 每页均提取到 52 行，即 50 行正文加页眉、页脚。
- 已渲染并目检操作手册 37 页和代码文档 65 页，共 102 页；未发现空白页、裁切、重叠或意外折行。

联调手册中的 32 张图片来自原案例，仅用于验证模板和分页，不能作为新项目的运行证明。正式一键生成会启动新软件并捕获其真实窗口。

## 当前机器上的限制

当前 C 盘空闲空间会随系统临时文件变化，最后两次入口预检为 541 至 606 MB，仍低于 700 MB硬门槛；完整生成建议预留 2 GB。因此生成器会在任何模型调用前停止，避免浪费额度。释放空间，或把整个“软件项目一键生成器”文件夹复制到空间充足且可写的磁盘后即可继续。

受此限制，本轮没有完成“安装 PyQt6 → 生成 10 个新模块 → 运行新软件 → 拍摄 32 张新截图 → 打包”的整链路实跑。该限制是环境容量，不是静默跳过；预检会明确报错。完成首次整链路实跑和人工验收前，不应把工具视为无人值守量产系统。

## macOS与多API验证边界

- 已实现macOS `.command` 启动器、生成软件的macOS启动器、LibreOffice文档转换、Homebrew Poppler查找、Finder打开结果和内置模板回退。
- 已实现Codex、Gemini、Grok、OpenAI及自定义OpenAI兼容接口选择；API Key不写入文件，进入生成软件测试子进程前会被移除。
- Gemini/Grok调用按官方OpenAI兼容Chat Completions和JSON Schema格式实现，使用模拟HTTPS响应完成离线测试，没有调用真实Key或产生API费用。
- 当前环境是Windows，因此不能声称macOS真实运行已经通过。需要在目标Mac上安装Python、LibreOffice和Poppler，运行预检并完成至少一个真实试单。

## 时间与 Token 评估

一份项目默认包含 1 次方案调用和 10 次模块调用；失败模块最多再调用 1 次。基于连接测试、5000 行以上领域源码要求和串行校验，保守预计：

- 顺利生成：约 1.5 至 3.5 小时机器时间，约 20 万至 35 万输入 Token、5 万至 9 万输出 Token。
- 有重试或网络波动：约 3 至 6 小时，Token 可能接近翻倍。
- 人工复核：建议至少 15 至 30 分钟，检查领域算法、全部截图、申请表字段和最终压缩包。

这是容量级估算，不是计费承诺。工具会在每次任务的 `耗时与Token.json` 中记录实际调用、耗时和 Token；ChatGPT 登录额度不等于 API 按 Token 账单。

按单份 20 元计算，除非只计 15 分钟左右的人工操作且通过率很高，否则不值得。超过 10 份后按每份 30 元，只有在首次整链路实跑通过、验收通过率接近 90% 以上、失败重做不由你承担时，才可能作为低人工占用的批处理兼职；若把机器占用、Codex 配额和返工也计入成本，收益仍偏低。

## 首次正式使用门槛

1. 释放或迁移后保证目标磁盘至少 700 MB，建议 2 GB。
2. 双击 `一键生成.cmd`，先运行“环境预检”。
3. 用一个真实软件名称完成整链路试单，人工检查 `待验收.zip`。
4. 记录实际耗时与 Token，再决定是否接 10 份以上批量单。

为释放空间，完成目检后已清理 102 张可再生的全分辨率页面 PNG 和一份 75 MB 的案例源码测试副本；PDF、DOCX、逐行映射、检查结果和 6 张全页总览图均保留。运行 `工具/verify_documents.py` 可从原案例重新生成这些临时材料。
