# 验证说明

版本：1.3.0。验证环境：Windows；目标环境：Windows 与 Apple 芯片 Mac。

## 生成器

- 12 项自检通过，覆盖方案契约、权限、模型接口模拟响应、跨平台启动入口、压缩包权限和通用业务表单。
- 通用项目的 10 个模块、表单操作和实际界面截图已离屏联调通过。普通用户输入不再依赖 JSON 编辑器。
- 使用离线夹具完成整条生成链路：10 个模块、37 张截图、42 页操作手册、65 页代码文档及待验收压缩包。此测试不调用模型。
- 单次运行的案例学习和模板元数据写入该次生成目录，不修改仓库内置知识库。
- 已删除 token 用量、兼职报价和时薪统计逻辑；模型接口只保留生成结果及必要的错误诊断。

## 验收案例

食品添加剂超量使用检测系统保留在仓库，作为生成结果示例；本轮不改造其业务界面。它的 93 项算法测试此前在 Windows 通过。样例规则仅供演示，不代表正式食品安全判定。

## 尚未实测

新版 Windows 独立启动入口在依赖检查处中断，修正后依用户要求未继续实跑。也没有在用户的 Apple 芯片 Mac 上实际安装依赖、登录 Codex CLI 或执行完整生成。Mac 启动与依赖路径已检查，但不能以 Windows 离线测试代替 Mac 实测。
