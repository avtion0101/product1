# 软件项目一键生成器 1.1

支持 Windows 和 macOS，可选择本机 Codex 登录、Google Gemini API、xAI Grok API、OpenAI API或其他支持 `chat/completions + json_schema` 的 OpenAI 兼容接口。

## 启动

### Windows

双击 `一键生成.cmd`。它使用 Codex 内置 Python，并在需要时把 PyQt6 安装到 `工具/.packages`。

### macOS

第一次使用先在终端执行：

    brew install python@3.12 python-tk@3.12 poppler
    brew install --cask libreoffice
    chmod +x 一键生成.command
    ./一键生成.command

以后可以双击 `一键生成.command`。启动器会检查 Python 3.11+ 和 Tk，建立 `.generator_venv` 并安装生成器自身的 Python 依赖。若系统没有 Homebrew，也可以分别安装 Python 3.11以上、带 Tk 的 Python 发行版、LibreOffice和Poppler。

仅运行仓库内的食品添加剂项目时，不需要 LibreOffice、Poppler 或生成器依赖；安装 Python 3.12 后进入 `食品添加剂超量使用检测系统` 目录，执行 `chmod +x 启动软件.command && ./启动软件.command`。首次运行会联网安装 PyQt6 与 OpenCV。

建议生成器所在磁盘至少有 2 GB 空闲空间，硬性预检门槛为 700 MB。

## 选择模型

窗口内可以直接选择提供商、模型和API地址：

- `codex`：复用当前电脑保存的 ChatGPT/Codex 登录，不需要API Key。Mac上先执行 `codex login`。
- `gemini`：默认地址 `https://generativelanguage.googleapis.com/v1beta/openai`，读取 `GEMINI_API_KEY`。
- `grok`：默认地址 `https://api.x.ai/v1`，读取 `XAI_API_KEY`。
- `openai`：默认地址 `https://api.openai.com/v1`，读取 `OPENAI_API_KEY`；模型由使用者填写。
- `custom`：填写支持OpenAI兼容Chat Completions及JSON Schema结构化输出的HTTPS地址、模型，读取 `CUSTOM_API_KEY`。

API Key可以在窗口中临时输入，也可以启动前设置环境变量。例如：

    export GEMINI_API_KEY="你的Key"
    export XAI_API_KEY="你的Key"

Windows PowerShell对应写法：

    $env:GEMINI_API_KEY="你的Key"
    $env:XAI_API_KEY="你的Key"

Key只保存在当前进程环境中，不写入 `settings.json`、日志、提示词或交付包。不要把真实Key填写进配置文件。

模型名称会随服务商更新；默认值来自制作时的官方接口示例，可直接在窗口中修改。API费用、速率限制和可用地区以各服务商账户为准。

## 案例和模板

Windows默认从 `D:\基础项目生成办法` 刷新五个原案例。把生成器复制到Mac后，如果没有该目录，会自动使用包内已经提炼的案例知识和两个模板副本；因此不要求把整个D盘案例一起复制。若要重新学习新的案例，在窗口选择案例目录或修改 `工具/settings.json` 的 `source_root`。

## 一键工作流

1. 读取内置或指定案例知识，规划10个业务模块。
2. 逐模块生成领域算法和测试，检查每模块至少500行有效代码。
3. 运行测试、启动PyQt软件，捕获登录注册及每模块输入、计算、复核截图。
4. 使用原模板生成操作手册、65页代码文档和申请表。
5. Windows使用Microsoft Word或LibreOffice；macOS使用LibreOffice转换PDF。
6. 使用Poppler渲染全部PDF页面，核对页数、代码行映射和异常分页。
7. 输出源码、文档、验收报告、耗时与Token统计以及 `待验收.zip`。

原案例目录不可用时不会重新训练模型，而是使用包内的结构化提炼结果。自动检查不等于委托方验收。

## 生成结果

位于 `生成结果/时间_软件名称_编号`：

- `交付包/软件名称`：源码、Windows和macOS启动入口、项目配置。
- 操作手册与代码的DOCX、PDF，申请表信息和代码行映射。
- 32张以上真实运行截图、全部页面检查图、验收报告。
- 每次模型调用的耗时与Token统计。
- `软件名称_V1.0_待验收.zip`。

## 失败与续跑

点击“继续上次任务”，选择含 `state.json` 的任务目录。成功方案和模块会读取缓存。更换提供商续跑是允许的，但同一项目最好保持同一模型，以减少风格差异。

“停止并保存”会停止本地检查或Codex子进程。普通HTTP API请求在网络库返回前可能无法立即取消。

## 命令行

Windows：

    一键生成.cmd --preflight
    一键生成.cmd --name "软件名称" --provider codex
    一键生成.cmd --name "软件名称" --provider gemini --model "gemini-3.8-flash"

macOS：

    ./一键生成.command --preflight
    ./一键生成.command --name "软件名称" --provider grok --model "grok-4.7"

`--base-url` 可覆盖所选API地址。命令行不接受API Key参数，避免Key进入shell历史。

## 当前验证边界

Windows上的Codex连接、Word/PDF、代码分页和启动入口已实测。Gemini/Grok客户端使用模拟HTTPS响应完成离线测试，没有消耗你的API额度。macOS启动、Homebrew路径和LibreOffice路径已实现，但当前没有在真实Mac硬件上运行；请先执行预检和一个试单，再用于批量交付。详见 `验证说明.md`。
